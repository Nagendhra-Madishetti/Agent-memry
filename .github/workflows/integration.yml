name: integration

# CI integration lane (CI-INT): spins up real backends (FalkorDB + Neo4j) and runs the full
# integration suite - heartbeat / loop / replay / multi-backend parity. Two service containers
# make this the most expensive job, so it runs WEEKLY and on demand (workflow_dispatch), NOT on
# every push, to keep private-repo Actions minutes in budget. The core un-knowing invariant is
# still gated per-PR by the cheaper `replay-invariant` job in ci.yml (FalkorDB only, no key).
# LLM-dependent tests run only when the MEMRY_LLM_* secrets are set; otherwise they skip.

on:
  workflow_dispatch:
  schedule:
    - cron: "0 6 * * 1"   # weekly (Mon 06:00 UTC)

permissions:
  contents: read

jobs:
  integration:
    runs-on: ubuntu-latest
    services:
      falkordb:
        image: falkordb/falkordb:latest
        ports:
          - 6379:6379
      neo4j:
        image: neo4j:5
        env:
          NEO4J_AUTH: neo4j/memrytest
        ports:
          - 7687:7687
          - 7474:7474
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-python@v5
        with:
          python-version: "3.11"
      - name: Install
        run: |
          python -m pip install --upgrade pip
          pip install -e ".[all,dev]"
      - name: Integration suite (real backends; LLM tests gated on secret)
        env:
          MEMRY_LLM_API_KEY: ${{ secrets.MEMRY_LLM_API_KEY }}
          MEMRY_LLM_BASE_URL: ${{ secrets.MEMRY_LLM_BASE_URL }}
          MEMRY_LLM_MODEL: ${{ secrets.MEMRY_LLM_MODEL }}
          MEMRY_NEO4J_URI: bolt://localhost:7687
          MEMRY_NEO4J_USER: neo4j
          MEMRY_NEO4J_PASSWORD: memrytest
        run: pytest tests/integration -q
